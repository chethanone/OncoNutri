import 'package:flutter/material.dart';

class DietRecommendationScreen extends StatelessWidget {
  const DietRecommendationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Diet Plan'), centerTitle: true),
      body: const Center(child: Text('Under Construction')),
    );
  }
}


